# CrabSense Local UI (kiosk / máy local)

Static UI để test API CrabSense trên máy local.

## Chạy

1. API: `http://localhost:5080` (project `src/CrabSenseBE.Api`)
2. UI:

```powershell
.\start.ps1
```

Mở `http://localhost:5500` — login `owner` / `Owner@123`.

## Seed IoT demo (optional)

```powershell
.\seed-demo-iot.ps1
```
